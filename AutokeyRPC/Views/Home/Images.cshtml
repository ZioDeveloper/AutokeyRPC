@model IEnumerable<AutokeyRPC.Models.RPC_FotoXTelaio>

@{
    ViewBag.Title = "Images";
}

<h2>Images</h2>

@Html.ActionLink("Torna alla lista", "DoRefresh")
<br />
<br />

@foreach (var item in Model)
{

    <table>
        <tr>
            <td>
                @*<img src="@Url.Content("~/UploadedFiles/" + item.NomeFile)" title="@Url.Content(item.NomeFile)" style='height: 100%; width: 100%; object-fit: contain'>*@
                @*<img src="@Url.Content("~/UploadedFiles/" + item.NomeFile)" title="@Url.Content(item.NomeFile)" alt="Image" style='height: 50%; width: 50%; object-fit: contain' />*@
                <img src="@Url.Content("~/UploadedFiles/" + item.NomeFile)" title="@Url.Content(item.NomeFile)" alt="Image" style='height: 400px; width: 400px; object-fit: contain' />
                <p>-</p>
            </td>
            <td>
                @Html.ActionLink("Delete Foto", "DeleteSingleFoto", new { id = item.ID, idTelaio = item.IDTelaio,picName = item.NomeFile }, new { @class = "deleteImg" })
            </td>
        </tr>
    </table>

}


